services:
  codefield:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
    container_name: codefield-app
